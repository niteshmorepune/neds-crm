# Support guide

You handle **tickets** (client issues) and **projects & tasks** (delivery work).

## Your dashboard
- **Open tickets** — how many are active.
- **SLA at risk** — tickets due soon or already overdue.
- **Open tickets by priority** — urgent / high / normal / low.

## 1. Tickets
Tickets come from clients (via their portal) or are raised by staff.

**Open a ticket** to see the conversation. Each ticket has a **priority** and an
**SLA due time** (when it should be resolved by) — urgent ones are due soonest.

**Replying:**
- Type in the reply box and send. A normal reply is **emailed to the client**.
- Tick **Internal note** to leave a note only staff can see (not emailed).
- If AI is enabled, **Draft with AI** writes a suggested reply from the
  conversation — edit it before sending. **Summarize thread** gives you a quick
  recap of a long ticket.

**Managing:** use the panel to change **status**, **priority**, or **assignee**,
attach files, and **Mark resolved** when done (the client is notified).

**SLA breaches:** if a ticket passes its due time while still open, managers are
alerted automatically — keep an eye on the "SLA at risk" number.

## 2. Projects (Project Updates)
A **Project** is created from a won deal. Open **Project Updates** to see your
projects, each with its client, service and tasks.

## 3. Tasks (Employee Task)
**Employee Task** is your task list.
- Update a task's **status** (To-do → In progress → Review → Done) as you work.
- Tasks have a **due date**; overdue ones are flagged.
- Attach files to a task when needed.

Completing tasks on time feeds your performance report, so keep statuses
current.

## 4. Calls
Use **☎ Log a call** (top bar) or **Calling** to record calls you make to
clients about their tickets or projects.

## Tips
- Always set the right **priority** on a ticket — it sets the SLA clock.
- Use **internal notes** for hand-offs between team members.
